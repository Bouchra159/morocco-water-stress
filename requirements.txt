# Core data handling
numpy>=1.26
pandas>=2.0
scipy>=1.11
requests>=2.28

# Raster and satellite imagery
rasterio>=1.3
xarray>=2023.1
netCDF4>=1.6          # reads the GRACE mascon files
affine>=2.4
pyproj>=3.5

# Vector data and spatial SQL
geopandas>=0.14
shapely>=2.0
duckdb>=1.0           # spatial SQL over the GeoPackage

# Analysis
scikit-learn>=1.3     # land-cover classification

# Figures and output
matplotlib>=3.7
Pillow>=10.0          # builds the timelapse GIF and the web story

# Web output
folium>=0.15          # the browsable basin map in src/cartography/build_map.py
mapclassify>=2.6
